import {} from "../ActionTypes"
    
    
const initialState = {
    folderLink: "",
    videos: [],
    processedVideoInfo: []
}
    
export default function reducer1(state = initialState, action){
    switch(action.type){
        default:
            return state
    }
}