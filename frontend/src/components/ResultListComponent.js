import React from "react";

export default class ResultList extends React.Component {

    constructor({items}){
        super({items});
    }

    render(){
        return(
            <div>

            </div>
        )
    }
}
